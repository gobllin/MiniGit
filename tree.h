#ifndef TREE_H
#define TREE_H

// The Map Maker's mission: Freeze the index into a permanent Tree file,
// and hand the Boss back the 40-character Hash ID of that new Tree.
int write_tree(char *tree_hash_out);

#endif